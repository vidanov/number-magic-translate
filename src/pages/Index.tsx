
import NumberTranslator from "../components/NumberTranslator";

const Index = () => {
  return (
    <div className="min-h-screen bg-gradient-to-b from-background to-secondary/50 flex flex-col items-center justify-center py-10">
      <NumberTranslator />
    </div>
  );
};

export default Index;
